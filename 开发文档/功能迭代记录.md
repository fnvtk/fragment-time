# 功能迭代记录

- 2026-07-07 15:16：从 GitHub 识别仓库 `fnvtk/fragment-time`，当前环境 `github.com:443` clone 超时，改用 GitHub API zip 下载源码并同步到当前目录。
- 2026-07-07 15:16：核验 NAS Gitea（`192.168.1.201:3000`）仓库 `fnvtk/fragment-time` 存在，但显示为空仓库，未发现提交内容。
- 2026-07-07 15:16：已完成当前目录初始化并设置远端：`origin=https://github.com/fnvtk/fragment-time.git`，`gitea=http://192.168.1.201:3000/fnvtk/fragment-time.git`。

## 2026-07-10 独立“碎片时间”小程序首页无数据修复

- 项目边界：本项目是存客宝旧系统 `spsj` 模块，不是 Soul 小程序；本轮未修改小程序代码。
- 真实 AppID：`wx6489c26045912fe1`；首页接口：`POST https://api.quwanzhi.com/api/app/spsj/task/getHotTask`。
- 故障一：KR 宝塔 PHP 7.2 socket `/tmp/php-cgi-72.sock` 丢失，Nginx 返回 502；已通过宝塔 API 重启 `php-fpm-72`恢复。
- 故障二：`api.quwanzhi.com` A 记录误指向 `42.194.245.239`，请求被 301 到聊天站；已仅将 `api` A 记录改为 KR `43.139.27.93`。
- 数据核验：`hx-private-server_v2.WechatApp_task` 中碎片时间应用有 32 条未删除任务、31 条热门任务。
- 验收：阿里 DNS `223.5.5.5` 与 Google DNS `8.8.8.8` 均解析到 `43.139.27.93`；首页接口连续两次 HTTP 200，`code=1`且 `data` 非空。

```text
碎片时间小程序
  → api.quwanzhi.com (43.139.27.93 / KR宝塔)
  → Nginx
  → PHP 7.2 / ThinkPHP spsj API
  → hx-private-server_v2 (WechatApp / WechatApp_task)
```
- 2026-07-10：排查“应用快照/Appshots”无法使用。当前项目未包含快照实现或相关调用，故障来源为 ChatGPT macOS 客户端权限；已打开 macOS“屏幕与系统音频录制”权限面板。按官方要求勾选 ChatGPT 后重启客户端即可恢复，未修改业务代码。

## 2026-07-11 碎片时间独立后台、后端与数据迁移

- 真实后端纠正：线上 `api.quwanzhi.com` 根目录为 KR `/www/wwwroot/self/quwan/api`，不是存客宝服务器的 `ckbapi.quwanzhi.com`。
- 小程序后端模块：`application/api/controller/app/spsj/`，依赖 13 张 `WechatApp*` / `WechatFriendRequestItem` 表。
- 已把完整 ThinkPHP 后端导出至本地 `backend-legacy/`，排除运行日志、上传目录和 Git 元数据。
- 已按 `WechatApp.id=1` 导出相关数据到 `data/fragment-time-data.sql`（58.9MB，权限 600，Git 忽略）。
- 本地独立 MariaDB：容器 `fragment-time-mysql`，端口 3310；导入后核验任务 103、用户 614、领取记录 38,685。
- Next 管理后台新增 MySQL 数据层及管理 API：仪表盘、任务、用户、数据包、提现、配置。
- 管理后台去除静态假数据；任务支持显示/隐藏，配置支持保存并立即影响同库小程序接口。
- 增加 HTTP Basic 登录保护；验收结果：未登录 401、登录 200、仪表盘 200。
- KR 宝塔部署：项目 `fragment_time_admin`，目录 `/www/wwwroot/self/fragment-time-admin`，Node 22，端口 3120。
- 新增 DNS `fragment-time.quwanzhi.com → 43.139.27.93`，绑定 Nginx 和泛域名证书；KR 内部 HTTPS 验收 401/200/200。
- 小程序源码仓库为 Gitee 私库，HTTPS 要求认证，本机 SSH Key 无权限，浏览器会话不可用；待提供 Gitee Token 后继续下载、接口核对、微信开发者工具验收与重新发布。
