# 碎片时间独立化需求

## 目标

- 下载 `cunkebao/fragmented-time-mini-program` 小程序源码。
- 从 KR 宝塔导出 `spsj` 后端代码与碎片时间专属数据。
- 建立独立管理后台，管理任务、用户、数据包、提现和小程序配置。
- 保证小程序、后台和数据库数据互通。
- 完成后部署到 KR 宝塔并通过 HTTPS 验收。

## 当前验收状态

- [ ] 下载 Gitee 私有小程序源码（等待 Gitee Token 或仓库授权）。
- [x] 确认线上 API：`https://api.quwanzhi.com/api/app/spsj/*`。
- [x] 导出线上 ThinkPHP 后端到 `backend-legacy/`。
- [x] 导出 13 张依赖表及碎片时间关联数据到本机受保护 SQL。
- [x] 建立独立 MariaDB，本地导入数据并核验数量。
- [x] 独立管理后台接入真实数据库。
- [x] KR 宝塔新增 Node 项目 `fragment_time_admin`。
- [x] 新域名、HTTPS、登录保护及数据接口验收通过。
- [ ] 小程序源码切换/核验后端配置并完成微信开发者工具全功能验收。
- [ ] 重新提交/发布小程序版本。

## 验收口径

- 管理后台未登录返回 401，登录后返回 200。
- 仪表盘、任务、用户、数据包、提现、配置均读取真实数据。
- 任务显示状态与小程序首页接口读取同一数据库。
- 小程序登录、任务领取、提交、排行榜、账单、提现、邀请和配置接口逐项通过。
